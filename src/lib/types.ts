type CartProduct = {
    productId: number,
    productName: string,
    quantity: number,
    price: number
}

type Product = {
    id: number,
    productName: string,
    image: string,
    description: string,
    cost: number
}

type NavLink = {
    href: string
    name: string
}